<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.webcomm.UploadCaseMapper">
	<insert id="addUploadCase" parameterType="UploadCase">
		INSERT INTO upload_case(caseNum, emiNum, creditCardNum, customerName, uploadTime)
		VALUES (#{caseNum}, #{emiNum}, #{creditCardNum}, #{customerName}, #{uploadTime})
	</insert>
</mapper>